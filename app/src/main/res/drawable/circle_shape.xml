<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">  <!-- Ключевой параметр! -->
    <solid android:color="@android:color/transparent" />  <!-- Цвет заливки -->
    <stroke
        android:width="1dp"
        android:color="@color/black" />
    <size
        android:width="100dp"
        android:height="100dp" />
</shape>